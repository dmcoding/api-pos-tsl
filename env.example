# Database Configuration
DATABASE_URL=postgresql://neondb_owner:npg_AoFjO2dMJeq4@ep-patient-wind-adku7ifz-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Alternative local PostgreSQL (for development)
# DATABASE_URL=postgresql://username:password@localhost:5432/pos_db

# SQLite for local development (fallback)
# DATABASE_URL=sqlite:///./pos.db

# JWT Configuration
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
refresh_token_expire_days=7

# Application Configuration
APP_NAME=POS API
APP_VERSION=1.0.0
DEBUG=True

# CORS Configuration
ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost:8080"]
ALLOWED_METHODS=["GET", "POST", "PUT", "DELETE", "PATCH"]
ALLOWED_HEADERS=["*"]

# Database Pool Configuration (for PostgreSQL)
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
